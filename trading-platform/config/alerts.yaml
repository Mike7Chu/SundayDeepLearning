# 알림(텔레그램) 규칙
# premium_pct: 원화 테더가(USDT/KRW) 기준. (국내KRW / (해외USDT * 테더가KRW) - 1) * 100.
#   양수=김프, 음수=역프. 테더가 없으면 은행 환율로 폴백.

# 감시할 (기준 국내거래소, 비교 해외거래소) 쌍
pairs:
  - base: upbit
    ref: binance
  - base: bithumb
    ref: binance

# 임계치 (둘 중 하나라도 충족 시 알림)
premium_high_pct: 3.0    # 김프 이 % 이상이면 알림
premium_low_pct: -1.5    # 역프 이 % 이하면 알림(음수)

# 현선: 국내현물 vs 해외선물 역프가 이 % 이하면 알림(국내현물 매수 + 해외선물 숏)
hyeonseon_low_pct: -1.0

# 펀비: |APY| 이 % 이상이면 과열 알림 / 거래소간 펀비차(%p) 이 이상이면 차익 알림
funding_apy_pct: 100.0
funding_spread_pct: 0.1

# 같은 (쌍,코인,방향) 알림 재발송 최소 간격(초) — 스팸 방지
cooldown_sec: 600

# 알림 평가 주기(초)
poll_interval_sec: 10
